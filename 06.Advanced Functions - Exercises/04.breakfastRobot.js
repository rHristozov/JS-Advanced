function solve() {
  let elements = {
    'protein': 0
  }

  let receipts = {
    'burger': {
      'carbohudrate': 5,
      'sadasd': 12
    }
  }

  const getError = (element) => ``;

  let commands = {
    'restock': (element, quality) = {
      // elelemts + receipts + getError
      //return

    }

    // function d (command) {
    //   //split string into words
    //   let commandsTokens = command.split('');
    
    }
  }
